"use strict";
// 	Number Eight: Write addition, subtraction, multiplication, and division operations that each result in the number 8. 
// Be sure to enclose your operations in print statements to see the results.
let result = 4 + 4; // use Addition operator +
console.log(`Addition operator + : ${result}.`);
let $result = 10 - 2; // use subtraction operator -
console.log(`Subtraction operator - : ${$result}.`);
let _result = 4 * 2; // use multiplication operator *
console.log(`Multiplication operaor * : ${_result}.`);
let result1 = 64 / 8; // use Division operator /
console.log(`Divition operator / : ${result1}.`);
